# API
fastapi==0.139.0
uvicorn[standard]==0.51.0
python-multipart==0.0.32
httpx==0.28.1

# Banco
# asyncpg >= 0.31: versões anteriores NÃO compilam em Python 3.13/3.14.
asyncpg==0.31.0
# psycopg (síncrono) é o driver das MIGRATIONS. asyncpg roda tudo como prepared
# statement e recusa múltiplos comandos num mesmo execute — e migration é DDL em
# bloco. Runtime = asyncpg; Alembic = psycopg. (ADR-002)
psycopg[binary]==3.3.4
SQLAlchemy[asyncio]==2.0.51
alembic==1.18.5
GeoAlchemy2==0.20.0

# Config
pydantic-settings==2.14.2

# Auth — só OAuth, sem senha: nada de passlib/bcrypt (ADR-005)
python-jose[cryptography]==3.5.0

# Object storage (ADR-001)
boto3==1.43.47

# Chaves primárias UUIDv7 (ADR-007)
uuid7==0.1.0

# Laudos em PDF
reportlab==5.0.0
qrcode[pil]==8.2

# Testes
pytest==9.1.1
pytest-asyncio==1.4.0

# Elasticsearch REMOVIDO — busca é Postgres FTS + pg_trgm (ADR-003).
